#include <iostream>
#include <vector>
#include <algorithm>

void переставити_нулі_спочатку(std::vector<double>& масив) {
    double мінімальний = масив[0];
    double сума_між_додатними = 0;
    bool перший_додатний_знайдений = false;

    // Знаходимо мінімальний елемент і обчислюємо суму між додатними елементами
    for (double елемент : масив) {
        мінімальний = std::min(мінімальний, елемент);

        if (елемент > 0) {
            if (!перший_додатний_знайдений) {
                перший_додатний_знайдений = true;
            } else {
                сума_між_додатними += елемент;
            }
        }
    }

    // Переставляємо всі елементи рівні нулю спочатку
    std::sort(масив.begin(), масив.end(), [](double a, double b) {
        return a == 0 || (b != 0 && a < b);
    });

    // Виводимо мінімальний елемент та суму між додатними
    std::cout << "Мінімальний елемент масиву: " << мінімальний << std::endl;
    std::cout << "Сума елементів між першим і останнім додатними: " << сума_між_додатними << std::endl;
}

int main() {
    std::vector<double> мій_масив = {3.0, -1.0, 2.0, 0.0, 5.0, -4.0, 7.0, 0.0}; // Зразок масиву, який можна змінити

    переставити_нулі_спочатку(мій_масив);

    std::cout << "Результат перестановки нулів спочатку:" << std::endl;
    for (double елемент : мій_масив) {
        std::cout << елемент << " ";
    }
    std::cout << std::endl;

    return 0;
}
